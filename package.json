{
  "name": "chatify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm i --save-dev @types/react @types/react-dom --legacy-peer-deps && npm run build && cd .. && cd backend && npm install --legacy-peer-deps && npm run start"
  },
  "author": "",
  "license": "ISC"
}
